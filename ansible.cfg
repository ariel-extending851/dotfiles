[defaults]
inventory = ansible/local/hosts.ini
roles_path = ansible/roles
stdout_callback = default
# Replaces removed community.general.yaml (v12.0.0+)
result_format = yaml
host_key_checking = false

[privilege_escalation]
become_ask_pass = false
