# ==============================================================================
# 99-devops.conf - Managed by system_tuning Ansible Role
# Process and File Limits for High-Throughput Dev Tools & Containers
# ==============================================================================

# User limits for open file descriptors (prevents "too many open files")
*               soft    nofile          {{ system_tuning_nofile_limit }}
*               hard    nofile          {{ system_tuning_nofile_limit }}
root            soft    nofile          {{ system_tuning_nofile_limit }}
root            hard    nofile          {{ system_tuning_nofile_limit }}

# User limits for max processes / threads
*               soft    nproc           {{ system_tuning_nproc_limit }}
*               hard    nproc           {{ system_tuning_nproc_limit }}
root            soft    nproc           {{ system_tuning_nproc_limit }}
root            hard    nproc           {{ system_tuning_nproc_limit }}
